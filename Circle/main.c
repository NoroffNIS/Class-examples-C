#include <stdio.h>
#include <stdlib.h>
#include <math.h>

#define PI 3.14159

int main()
{
        ax^2+bx+c=0

}

/*
double radius = 3;
    double diameter = radius*2;
    double area = PI * pow(radius,2);

    printf("Area of a circle with %.2f radius is: %.2f", radius, area);
    return 0;

*/
